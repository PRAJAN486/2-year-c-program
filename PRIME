#include<stdio.h>
void main()
{
    int a,count=0;
    printf("enter any num");
    scanf("%d",&a);
    for(int i=0;i<a;i++)
    {
    if(a%i==0)
       count=count+1;
    }
    if(count=0)
      printf("PRIME");
    else
      printf("COMPOSITE");
} 
